using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
Will recive event to begin minigame and child classes should then setup needed ui
Minigames should inherit from this class for simplicity
*/

public class Minigame : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
