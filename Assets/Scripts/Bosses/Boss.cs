using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
Generic boss class, for before combat or minigame, will load dialouge file and control idle behaviour, when combat/minigame begins hand off control to unique combat boss class
*/

public class Boss : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
