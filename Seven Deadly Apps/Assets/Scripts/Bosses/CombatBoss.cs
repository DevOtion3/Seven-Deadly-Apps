using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
Handles transition from idle boss to combat/minigame
Boss combat class should inherit from this class in order to easily transtion between states
*/

public class CombatBoss : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
